# Phase 1 Notebooks

| Notebook | Topic | Outcome |
| --- | --- | --- |
| `01_token_counter.ipynb` | Tokens, cost estimation | Estimate cost before sending requests |
| `02_prompt_playground.ipynb` | Temperature, models, sampling | Compare outputs across settings |

Open with: `jupyter notebook` (after `pip install notebook openai tiktoken python-dotenv`).

> No API key? Swap the OpenAI calls for Ollama (`openai` client pointed at
> `http://localhost:11434/v1`) — see the docker-compose at repo root.
